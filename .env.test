# ── Test environment variables ────────────────────────────────────────────────
# Safe to commit — these are test credentials only.

# Database
POSTGRES_DB=smartfarm_test
POSTGRES_USER=smartfarm_test
POSTGRES_PASSWORD=smartfarm_test_pass

# App
SECRET_KEY=test_secret_key_not_for_production_use_only_32chars
ALLOWED_ORIGINS=http://localhost:8084
DEBUG=false

# AI (leave blank if not needed for tests)
ANTHROPIC_API_KEY=
